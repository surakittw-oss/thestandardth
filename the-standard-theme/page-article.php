<?php
/**
 * Template Name: THE STANDARD — Article
 *
 * Assign this template to a Page with the slug "article" so the homepage
 * cards (which link to <article-base>?id=<article-id>) resolve here.
 *
 * @package the-standard
 */

get_header();
get_template_part( 'template-parts/article-app' );
get_footer();
